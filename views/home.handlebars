<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Products</title>
</head>
<body>
  <h1>Product List</h1>
  <ul>
    {{#each products}}
      <li>{{this.name}} - {{this.price}} - {{this.category}}</li>
    {{/each}}
  </ul>
</body>
</html>
